%tr.page{:id => "page_#{page.id}"}
  %td= page.name
  %td= page.online
  %td
    = link_to(t('destroy'), admin_page_path(page), :method => :delete)
    = ' / '
    = link_to(t('edit'), edit_admin_page_path(page))
    = ' / '
    = link_to(t('show'), admin_page_path(page))
