- title(@category.name, false)
.row
  - @category_children.each do |category|
    = link_to(category_pictures_path(category)) do
      .span5.center.category
        .image_container= image_tag(category.pictures.first.image.url(:small))
        = category.name